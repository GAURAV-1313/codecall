# Learn automatic recommendation

Before the normal final response for a completed implementation, read the
installed learn skill's `references/trigger-policy.md` and evaluate the change.
When its outcome is `recommend`, present Start Learning / Skip and wait. Do not
teach until Start Learning. For `optional` or `skip`, finish normally; `/learn`
always remains available.
